import React from "react"

const UserContext = React.createContext()

export default UserContext;

/* <UserContext>
    <Login>
        <Card>
            
        </Card>
    </Login>
</UserContext> */
//context-provider ese banate hai